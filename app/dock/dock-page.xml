<Page
    class="page"
    navigatingTo="onNavigatingTo"
    xmlns="http://schemas.nativescript.org/tns.xsd">

    <ActionBar class="action-bar">
        <NavigationButton ios:visibility="collapsed" icon="res://menu" tap="onDrawerButtonTap"></NavigationButton>
        <ActionItem icon="res://navigation/menu"
            android:visibility="collapsed"
            tap="onDrawerButtonTap"
            ios.position="left">
        </ActionItem>
        <Label class="action-bar-title" text="Dock"></Label>
    </ActionBar>

    <DockLayout id="container" class="layout-container" stretchLastChild="true">
        <Button dock="Left" text="left" backgroundColor="lightPink" tap="onTap" />
        <Button dock="Top" text="top" backgroundColor="lightBlue" tap="onTap" />
        <Button dock="Right" text="right" backgroundColor="lightGreen" tap="onTap" />
        <Button dock="Bottom" text="bottom" backgroundColor="lightGray" tap="onTap" />
        <Button text="fill" backgroundColor="lightYellow" tap="onTap" />
    </DockLayout>

</Page>
